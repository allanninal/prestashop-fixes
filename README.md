# prestashop-fixes

Small, focused scripts that detect and repair the everyday problems that hit real PrestaShop stores. Every fix ships in **both Python and Node.js**, is **safe by default** (a `DRY_RUN` flag that defaults to `true`, so it reports before it writes), and has a **pure decision function** with unit tests.

Each fix has a full write-up with diagrams on **[allanninal.dev/prestashop](https://www.allanninal.dev/prestashop/)**.

## How the scripts authenticate

The scripts talk to the PrestaShop **Webservice API**. Enable it (Advanced Parameters > Webservice) and create a key:

```bash
export PRESTASHOP_URL="https://your-shop.example.com"
export PRESTASHOP_WS_KEY="your webservice key"
export DRY_RUN="true"
```

The key is sent as the HTTP Basic username (blank password) to `/api/...` resources.

## The fixes

| Fix | What it does | Type | Guide |
| --- | --- | --- | --- |
| [Stock quantity goes negative after ordering an out of stock product](negative-stock-quantity-after-order/) | Ordering a zero stock item drives stock_available quantity negative; script scans for negative quantities and reconciles. | Reconciler | [guide](https://www.allanninal.dev/prestashop/negative-stock-quantity-after-order/) |
| [Physical quantity, reserved quantity, and virtual quantity fall out of sync](stock-quantity-formula-drift/) | Physical quantity should equal virtual plus reserved quantity; script recomputes from order details and flags violations. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/stock-quantity-formula-drift/) |
| [Product quantity wrongly doubled or changed when order status changes](stock-quantity-corrupted-on-status-change/) | Changing an order state can double increment or corrupt stock; script diffs stock movements against expected per state change. | Reconciler | [guide](https://www.allanninal.dev/prestashop/stock-quantity-corrupted-on-status-change/) |
| [Duplicate key error when updating stock quantity concurrently](stock-available-duplicate-key-error/) | Concurrent quantity updates throw a duplicate entry error on the stock table; script finds and merges duplicate stock rows. | Reconciler | [guide](https://www.allanninal.dev/prestashop/stock-available-duplicate-key-error/) |
| [Negative stock quantities appear even with backorders disabled](negative-stock-despite-backorder-denied/) | Stock quantity goes negative in single store and multistore setups despite deny backorder settings; script scans all shops and reports or clamps. | Reconciler | [guide](https://www.allanninal.dev/prestashop/negative-stock-despite-backorder-denied/) |
| [Reserved stock quantity drifts from actual pending orders](reserved-quantity-drift/) | Reserved quantity stops matching real pending order units and never releases after cancellation; script recomputes from open orders. | Reconciler | [guide](https://www.allanninal.dev/prestashop/reserved-quantity-drift/) |
| [Orphaned stock rows remain after a combination is deleted](orphaned-stock-rows-after-combination-delete/) | Deleting a product combination leaves its stock row behind, skewing total quantity; script diffs stock rows against live combinations and deletes orphans. | Reconciler | [guide](https://www.allanninal.dev/prestashop/orphaned-stock-rows-after-combination-delete/) |
| [Combination stock quantities do not sum to the product level total](combination-quantity-sum-mismatch/) | Backoffice shows a wrong aggregate quantity for products with variants; script sums combination stock and flags mismatches against the product total. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/combination-quantity-sum-mismatch/) |
| [Order detail stock quantity fields disagree with the real order quantity](order-detail-stock-quantity-inconsistent/) | A stock reconciliation field can report zero while the ordered quantity is one; script compares the two fields per order line via the API. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/order-detail-stock-quantity-inconsistent/) |
| [Negative quantities recorded on backorder-paid orders](negative-quantity-on-backorder-orders/) | Orders placed as backorder can leave order lines and stock with negative quantities; script scans for negative quantity rows and normalizes them. | Reconciler | [guide](https://www.allanninal.dev/prestashop/negative-quantity-on-backorder-orders/) |
| [Product quantity field via webservice always reads or writes as zero](webservice-product-quantity-always-zero/) | The product resource ignores real stock, always showing quantity zero; script reads and writes the stock endpoint instead and flags stale zero rows. | Reconciler | [guide](https://www.allanninal.dev/prestashop/webservice-product-quantity-always-zero/) |
| [Stock update via webservice does not sync back to the product quantity field](webservice-stock-update-not-synced-to-product/) | Updating stock through the API leaves the legacy product quantity field stale or stuck at zero; script cross checks both and reconciles. | Reconciler | [guide](https://www.allanninal.dev/prestashop/webservice-stock-update-not-synced-to-product/) |
| [PATCH request to the stock endpoint is silently redirected and dropped](stock-patch-silently-dropped/) | PATCH calls get redirected to a GET, so the quantity write never persists; script verifies the update actually applied after every write. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/stock-patch-silently-dropped/) |
| [Updating stock via the API can delete the linked product combination](stock-update-deletes-combination/) | A stock update call for a variant can wipe out the referenced combination entirely; script detects stock rows whose combination no longer resolves. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/stock-update-deletes-combination/) |
| [Stock hooks and back in stock alerts never fire on webservice quantity updates](webservice-stock-update-skips-hooks-and-alerts/) | Restocking through the API skips internal hooks so caches and customer back in stock emails never trigger; script compares stock transitions against notification logs. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/webservice-stock-update-skips-hooks-and-alerts/) |
| [Product created via webservice is invisible on the storefront](webservice-product-invisible-on-storefront/) | API created products show active in the backoffice but never appear on the front end due to missing shop or category links; script re-checks and repairs associations. | Repair | [guide](https://www.allanninal.dev/prestashop/webservice-product-invisible-on-storefront/) |
| [Product visibility setting silently reverts after being changed via API](product-visibility-reverts/) | Setting a product visibility flag to hidden can silently reset to visible everywhere; script compares intended versus actual visibility and reapplies it. | Reconciler | [guide](https://www.allanninal.dev/prestashop/product-visibility-reverts/) |
| [Products disappear from the storefront days after being added by API](products-disappear-days-after-api-creation/) | API created products vanish after a delay from indexing or category link decay; script polls active, visibility, and category links over time. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/products-disappear-days-after-api-creation/) |
| [Setting a default combination via API throws a duplicate key error](webservice-default-combination-duplicate-key/) | Marking a new variant as default through the API can error out, leaving more than one variant flagged default; script finds combinations with a wrong default count. | Repair | [guide](https://www.allanninal.dev/prestashop/webservice-default-combination-duplicate-key/) |
| [Product images updated via API do not create per shop associations in multistore](webservice-images-missing-shop-association/) | Uploading images through the API skips writing the per shop association so images are missing on other shops; script checks each shop images endpoint against the association table. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/webservice-images-missing-shop-association/) |
| [Category created via webservice ignores shop scoping in multistore](webservice-category-ignores-shop-scope/) | Posting a category through the API associates it with every shop instead of the intended one; script lists shop associations and flags categories in unintended shops. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/webservice-category-ignores-shop-scope/) |
| [Order history record is missing for the order's current state](order-history-missing-for-current-state/) | An order shows a current state with no matching history row on record, often after an upgrade; script compares current state to the latest history entry and backfills it. | Reconciler | [guide](https://www.allanninal.dev/prestashop/order-history-missing-for-current-state/) |
| [Order current state field goes stale after order history is edited](order-current-state-stale-after-history-edit/) | Deleting or editing history rows leaves the order's stored current state out of date; script recomputes it from the latest history row and patches it via the API. | Reconciler | [guide](https://www.allanninal.dev/prestashop/order-current-state-stale-after-history-edit/) |
| [Order history entries appear out of chronological order](order-history-out-of-chronological-order/) | History rows can be inserted with the wrong timestamp, showing an old status as current; script sorts history by date and flags orders that disagree with the true latest entry. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/order-history-out-of-chronological-order/) |
| [Duplicate order history rows created for a single status change](duplicate-order-history-rows/) | Setting an order status, including through the webservice or with the same state twice, can insert a second identical history row; script finds and dedupes repeated consecutive entries. | Reconciler | [guide](https://www.allanninal.dev/prestashop/duplicate-order-history-rows/) |
| [Order gets stuck permanently on one status with no history advancing](order-stuck-on-stale-status/) | Status change calls appear to succeed but the order's history never advances past an old state; script polls orders whose state has not changed in an implausible number of days. | Reconciler | [guide](https://www.allanninal.dev/prestashop/order-stuck-on-stale-status/) |
| [Order created via webservice with no current state set at all](webservice-order-created-without-state/) | Orders created through the API can end up with no state and no history rows; script scans for stateless orders and backfills the correct history entry. | Reconciler | [guide](https://www.allanninal.dev/prestashop/webservice-order-created-without-state/) |
| [Order reaches a paid state despite the product being out of stock](paid-order-despite-out-of-stock-product/) | An order can be marked paid even though its product shows zero stock and backorders are denied; script cross checks order lines against stock at time of order. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/paid-order-despite-out-of-stock-product/) |
| [Order created in the wrong state when the amount paid does not match the total](order-state-mismatch-on-amount-paid/) | Order validation can create an order in a normal state instead of an error state when paid amount and total disagree; script flags orders where the two totals mismatch. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/order-state-mismatch-on-amount-paid/) |
| [Order payment row duplicated for certain order state configurations](duplicate-order-payment-row/) | Validating an order on a state with both paid and validated flags enabled can create two payment rows for one order; script finds orders with duplicate payment amounts. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/duplicate-order-payment-row/) |
| [Total paid real field doubles after a partial payment update](total-paid-real-doubled/) | The real amount paid field can be written twice, showing double the actual payment; script sums payment rows and flags mismatches against the stored total. | Reconciler | [guide](https://www.allanninal.dev/prestashop/total-paid-real-doubled/) |
| [Orders created via webservice land in a payment error state](webservice-order-payment-error-mismatch/) | API created orders can mismatch total paid against the payment record, forcing a payment error state; script validates totals before and after creation and corrects the payment row. | Reconciler | [guide](https://www.allanninal.dev/prestashop/webservice-order-payment-error-mismatch/) |
| [Order total does not match the sum of its order line totals](order-total-mismatch-with-line-sum/) | Line totals can drift from the stored order total from rounding or module bugs, especially after editing; script sums order lines and diffs against the stored total. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/order-total-mismatch-with-line-sum/) |
| [Order total wrong after cancelling a line item on an order with a voucher](order-total-wrong-after-line-cancel-with-voucher/) | Removing a product from an order that has a cart rule applied leaves the total incorrect versus a recalculated sum; script detects the delta via the API. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/order-total-wrong-after-line-cancel-with-voucher/) |
| [Split orders show mismatched totals and wrong shipping cost](split-order-mismatched-shipping/) | When one cart splits into multiple orders, one split order can get the wrong carrier and shipping cost; script compares carrier id against shipping charged. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/split-order-mismatched-shipping/) |
| [Refunded quantity exceeds the originally ordered quantity](refunded-quantity-exceeds-ordered/) | Repeated partial refunds combined with manual edits can push refunded quantity above ordered quantity, risking a data error; script audits order lines for refunded greater than ordered. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/refunded-quantity-exceeds-ordered/) |
| [Partial refund accepted for more than the original line amount](refund-amount-exceeds-line-total/) | The backoffice and API let a refund amount exceed the original line total with no server side cap; script scans refund records against line totals for overage. | Reconciler | [guide](https://www.allanninal.dev/prestashop/refund-amount-exceeds-line-total/) |
| [Credit slip amount ignores the original voucher discount](credit-slip-ignores-voucher-discount/) | Refunding a discounted order can generate a credit slip that does not subtract the original discount, overstating the refund; script recomputes the expected refund and flags mismatches. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/credit-slip-ignores-voucher-discount/) |
| [Refund created via API does not update the order's refunded quantity](api-refund-not-updating-order-line/) | Creating a credit slip through the API can leave the order line's refunded quantity stale; script compares the requested refund against the stored refunded quantity. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/api-refund-not-updating-order-line/) |
| [Duplicate invoice numbers issued across different orders](duplicate-invoice-numbers/) | Two distinct orders can receive the same invoice number under concurrent checkout load; script queries orders for invoice number collisions. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/duplicate-invoice-numbers/) |
| [Invoice number stays empty after an order is marked shipped via API](invoice-number-missing-after-api-status-change/) | An API driven status change can generate the shipment state without populating the order's invoice number; script finds invoiced states with a missing invoice number. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/invoice-number-missing-after-api-status-change/) |
| [Single use voucher redeemed more than its allowed quantity](voucher-redeemed-beyond-quantity-limit/) | A voucher limited to one use can be redeemed multiple times under concurrent checkouts; script counts orders against the voucher's quantity limit and flags overuse. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/voucher-redeemed-beyond-quantity-limit/) |
| [Expired voucher still usable and left attached to orders](expired-voucher-still-applied/) | A voucher past its expiry date can still be loaded, applied, and paid on an order; script scans carts and orders for voucher associations past their expiry date. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/expired-voucher-still-applied/) |
| [Per customer voucher limit ignored for guest checkouts sharing an email](voucher-per-user-limit-ignored-for-guests/) | Guests can reuse the same voucher without limit across repeated guest orders under one email; script counts voucher usage grouped by email to flag overuse. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/voucher-per-user-limit-ignored-for-guests/) |
| [Free gift line quantity doubles when unrelated cart items are removed](free-gift-quantity-doubles/) | An automatic free product rule can bump the gift line's quantity to two when other items are removed from the cart; script inspects carts for gift lines with quantity above one. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/free-gift-quantity-doubles/) |
| [System generated vouchers without a code cannot be deleted and pile up](orphaned-codeless-vouchers-accumulate/) | Automatically generated cart rules with no code accumulate as orphaned unusable records; script lists codeless cart rules with no remaining uses for cleanup. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/orphaned-codeless-vouchers-accumulate/) |
| [Free shipping voucher fails to zero out the shipping cost](free-shipping-voucher-not-applied/) | A voucher marked free shipping can still leave a nonzero shipping line in the cart or order total; script recomputes expected total and flags carts where shipping was not zeroed. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/free-shipping-voucher-not-applied/) |
| [Duplicate customer accounts created from the same email during guest checkout](duplicate-customer-accounts-same-email/) | Guest checkout and account conversion flows can create two customer rows sharing one email; script queries customers by email for a count above one and flags for merge. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/duplicate-customer-accounts-same-email/) |
| [Guest order becomes untrackable after registering an account with the same email](guest-order-untrackable-after-registration/) | A guest order's customer link becomes orphaned once the guest email registers as a full account; script joins orders to customers by email to find and relink orphans. | Reconciler | [guide](https://www.allanninal.dev/prestashop/guest-order-untrackable-after-registration/) |
| [Product left without a valid default category after category deletion](product-missing-default-category-after-deletion/) | Deleting a category can leave products pointing at a default category id that no longer exists; script finds affected products and reassigns a valid default via the API. | Reconciler | [guide](https://www.allanninal.dev/prestashop/product-missing-default-category-after-deletion/) |
| [Product default category silently changed or lost after a catalog import](default-category-overwritten-by-import/) | CSV or bulk import can overwrite a product's default category unexpectedly, especially in multistore; script compares default category before and after import batches. | Reconciler | [guide](https://www.allanninal.dev/prestashop/default-category-overwritten-by-import/) |
| [Product's default category is not among its assigned categories](default-category-not-in-assigned-categories/) | A product's default category id can drift to a category the product is not actually linked to; script cross checks category links against the default category field. | Reconciler | [guide](https://www.allanninal.dev/prestashop/default-category-not-in-assigned-categories/) |
| [Products or categories orphaned outside the root category tree](orphaned-categories-outside-root-tree/) | Deleting the home category or bad imports can leave categories and products detached from the tree, making them invisible; script walks the category tree via the API to find unreachable nodes. | Reconciler | [guide](https://www.allanninal.dev/prestashop/orphaned-categories-outside-root-tree/) |
| [Duplicate product reference or SKU allowed across different products](duplicate-product-reference-sku/) | PrestaShop has no built in uniqueness check on the reference field; script queries products grouped by reference and reports collisions across different product ids. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/duplicate-product-reference-sku/) |
| [Product duplication leaves a broken partial product after an error](broken-product-duplicate-after-error/) | Duplicating a product with combinations can fail partway through yet still create an incomplete duplicate; script finds recently created products with missing combinations or features. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/broken-product-duplicate-after-error/) |
| [Product has no default combination, causing price to display as zero](missing-default-combination-zero-price/) | A product with variants but no valid default combination id shows a listing price of zero; script scans for missing or invalid default combination ids and repairs them. | Repair | [guide](https://www.allanninal.dev/prestashop/missing-default-combination-zero-price/) |
| [Duplicate key error creating a default combination per shop in multistore](multistore-default-combination-duplicate-key/) | Creating or converting a default variant on a second shop can throw a duplicate key error, leaving inconsistent default flags; script checks each product for more than one default combination per shop. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/multistore-default-combination-duplicate-key/) |
| [Cover image missing or duplicated, breaking storefront image links](cover-image-missing-or-duplicated/) | A product can end up with zero or more than one image flagged as cover, breaking the display link or the upload via API; script finds products with a wrong cover image count and flags or fixes them. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/cover-image-missing-or-duplicated/) |
| [Combination resolved from the wrong shop context in multistore](wrong-shop-combination-resolved/) | The product assembler can attach a variant belonging to a different shop, showing wrong price or minimum quantity; script fetches combinations per shop and flags shop mismatches. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/wrong-shop-combination-resolved/) |
| [Orphaned combinations remain linked after a product is removed from a shop](orphaned-combinations-after-shop-removal/) | Removing a product from one shop in multistore leaves its variants still linked to that shop's association table; script cross checks combinations against active shop associations. | Reconciler | [guide](https://www.allanninal.dev/prestashop/orphaned-combinations-after-shop-removal/) |
| [Currency exchange rate update in one shop overwrites another shop's rate](exchange-rate-overwritten-across-shops/) | Manually updating an exchange rate in one shop context can clobber the rate used by other shops; script snapshots rates per shop and detects unintended overwrites. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/exchange-rate-overwritten-across-shops/) |
| [Catalog listing shows the wrong price when currency differs per shop](multistore-listing-price-mismatch/) | The product listing can pull a different shop's price than the single product page shows; script compares listing price against single product price per shop via the API. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/multistore-listing-price-mismatch/) |
| [Tax calculation uses the wrong shop's default country rate](multistore-wrong-country-tax-rate/) | Order totals can compute tax using another shop's default country rate instead of the customer's actual country; script recomputes expected tax per order and compares to the stored total. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/multistore-wrong-country-tax-rate/) |
| [Catalog price rules silently skip products that already have a specific price](catalog-rule-skipped-by-specific-price/) | A catalog wide discount rule does not apply to products that already carry an individual price override; script finds products excluded from active catalog rules. | Reconciler | [guide](https://www.allanninal.dev/prestashop/catalog-rule-skipped-by-specific-price/) |
| [Wrong price selected when multiple pricing rules overlap for a customer group](specific-price-priority-wrong/) | Price rule priority logic can pick a higher price over a better one available for the customer's group or currency; script recalculates the best price and compares it to the API reported price. | Reconciler | [guide](https://www.allanninal.dev/prestashop/specific-price-priority-wrong/) |
| [Wrong tax rule applied on the product page for the delivery country](wrong-tax-rule-for-delivery-country/) | A product price can show a tax rate belonging to an unrelated country instead of the delivery address country; script requests price per country and flags tax rule mismatches. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/wrong-tax-rule-for-delivery-country/) |
| [Order stored with no carrier and free shipping the customer was not entitled to](order-missing-carrier-unearned-free-shipping/) | A checkout race can store an order with no carrier assigned and zero shipping despite no free shipping entitlement; script scans orders for a missing carrier alongside zero shipping. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/order-missing-carrier-unearned-free-shipping/) |
| [Editing a carrier assigns it a new id and orphans old references](carrier-edit-creates-new-id/) | Editing a carrier in the backoffice can create a new carrier id instead of updating in place, orphaning orders that reference the old id; script flags orders whose carrier id resolves to a deleted row. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/carrier-edit-creates-new-id/) |
| [Order's carrier reference becomes invalid after editing order lines](order-carrier-invalid-after-line-edit/) | Editing an order's products or quantities can invalidate its linked carrier id; script validates that every order's carrier id still exists and is not deleted. | Diagnostic | [guide](https://www.allanninal.dev/prestashop/order-carrier-invalid-after-line-edit/) |
| [Order created via webservice drops the carrier and shipping cost fields](webservice-order-drops-carrier-shipping/) | Posting a new order through the API can ignore submitted carrier and shipping fields, causing wrong invoices; script diffs submitted versus stored carrier and shipping values. | Repair | [guide](https://www.allanninal.dev/prestashop/webservice-order-drops-carrier-shipping/) |
| [Duplicate friendly URL slug collides across different products or categories](duplicate-friendly-url-slug/) | Two products or categories can share an identical URL slug so only one ever resolves correctly; script queries for duplicate slugs per language and renames the collision. | Reconciler | [guide](https://www.allanninal.dev/prestashop/duplicate-friendly-url-slug/) |
| [Friendly URL field accepts invalid values instead of a proper slug](invalid-friendly-url-format-accepted/) | The API or backoffice can save a full URL or invalid characters into the slug field, breaking rewrite rules; script validates slug format across the catalog and fixes bad entries. | Fix | [guide](https://www.allanninal.dev/prestashop/invalid-friendly-url-format-accepted/) |
